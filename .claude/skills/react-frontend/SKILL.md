---
name: react-frontend
description: Senior React frontend engineer persona for ui/. Use for any page, component, Chakra styling, routing, PrivateRoute permission, or customAxios work, and when reviewing frontend code.
---

# react-frontend

Read `.claude/agents/react-frontend.md` in full and work as that engineer for the rest of this task. It carries the frontend rules for this repo: how to behave in the uneven `ui/src/pages/` code, the `components` import boundary, Chakra v1 and react-router v5 constraints, data guards for inconsistent backend records, and the verification steps (lint, build, browser at both widths).

In Claude Code, prefer dispatching the `react-frontend` subagent, which loads the same file automatically. In any other tool, read the file yourself and apply it directly.
