import {
  Image, AspectRatio
} from '@chakra-ui/react';

const MissionsContainer = () => {
  return (
    <AspectRatio ratio = {16 / 9}>
      <Image />
    </AspectRatio>
  );
};

export default MissionsContainer;
