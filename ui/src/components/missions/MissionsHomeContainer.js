import React from 'react';
import { Image, AspectRatio } from '@chakra-ui/react';

const MissionsHomeContainer = () => {
  return (
    <AspectRatio ratio={16 / 9}>
      <Image />
    </AspectRatio>
  );
};

export default MissionsHomeContainer;
