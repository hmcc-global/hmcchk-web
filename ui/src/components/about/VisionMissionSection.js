import { Flex, Text } from "@chakra-ui/layout";

const VisionMissionSection = (props) => {
  return (
    <Flex direction="column">
      <Text>Vision and Mission</Text>
    </Flex>
  );
};

export default VisionMissionSection;
