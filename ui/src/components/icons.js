/**
 * The Chakra icons boundary — the only file allowed to import '@chakra-ui/icons'.
 * Kept separate from the main barrel to avoid name collisions (both packages
 * export e.g. `Icon`). Consumers: `import { ChevronLeftIcon } from 'components/icons'`.
 */
export * from '@chakra-ui/icons';
